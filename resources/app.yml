resources:
  apps:
    inspectiq_mbi:
      name: inspectiq-mbi
      description: InspectIQ — Michael Baker International Infrastructure Knowledge Assistant
      source_code_path: ../app
      resources:
        - name: sql-warehouse
          sql_warehouse:
            id: ${var.warehouse_id}
            permission: CAN_USE
        - name: serving-endpoint
          serving_endpoint:
            name: databricks-claude-sonnet-4-5
            permission: CAN_QUERY
